这是一道简单题，要求原地移除数组内的指定元素val。
第一个想法是，设置两个指针，指针1从下标0开始，指针2用下标nums.length-1开始。指针1寻找等于val的位置，指针2寻找不等于val的位置，一旦两个指针都找到相应的下标，就交换两个下标的数值，达到将指定元素移动到数组末尾的目的。
同时，在寻找过程中给nums.length减去等于val的下标个数，就是要返回的值。
通过提交发现这个做法会忽略很多边界情况，添加边界情况后提交通过。（LC0027_V1.java）

后来发现还有更简洁的做法（LC0027_V2.java），因为题目注明了不需要考虑数组中超出新长度后面的元素，因此直接将等于val位置的数值替换成后面的非val数据即可。
