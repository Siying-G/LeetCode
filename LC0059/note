这道题目标注的是中等难度，实际不涉及算法，按照规则循环一遍即可。
我的想法是，设置一个变量direction表示方向，0表示向右，1表示向下，2表示向左，3表示向上。在每次要寻找新的位置时，先根据边界条件判断是否需要改变方向，否则就在现有方向上移动到下一个位置。
提交通过，用时0ms，但内存消耗较多。
查看评论区发现了更有效的做法，即在当前方向上判断起始位置和终止位置，再给起始位置和终止位置之间的位置全部赋值，这样可以减少判断边界条件的次数，效率更高。
