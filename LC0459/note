给定一个非空的字符串，判断它是否可以由它的一个子串重复多次构成.
因此首先可以判断子串的长度len，一定有s.length() % len == 0，通过长度判断可以减少很多冗余操作。
字串长度最小是1，最长是s.length()-1；
因此考虑从1至s.length()-1遍历，寻找每个长度的可能性，
在判断长度n时，首先截取s.substring(0,0+len)，这是子串内容，再用for循环，令i每次加n，判断s.substring(i,i+len)是否和s.substring(0,0+len)相同，若有不同则停止循环，若循环正常结束即可返回true。
