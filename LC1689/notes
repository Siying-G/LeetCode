  第一眼看到题目，想到查找字符串n中最小的非零字符，再创建一个字符串midVar，若n中相应下标的数字非0，此处midVar中的字符为1，否则为0.
  编写完毕提交，发现int类型越界（示例 3测试），于是将第一段中所提方法的数字均换成字符串，字符串操作代替加减操作
  第二次提交（LC1689_V1.java），报出了超时错误，发现此时的测试样例字符串非常长，占满了整个屏幕
  无奈之下，猛然发现，寻找的数字其实就是将字符串n转为数字后，其中最大的数字，也就是输出n的最大元素即可。（LC1689_V2.java）
