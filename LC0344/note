这是一道简单题，很容易用双指针法做出解答。
但需要注意的是，交换两个位置上的值，可以使用位运算，使用异或逻辑编写更快：
s[start] ^= s[end];//构造 a ^ b 的结果，并放在 a 中
s[end] ^= s[start];//将 a ^ b 这一结果再 ^ b ，存入b中，此时 b = a, a = a ^ b
s[start] ^= s[end];//a ^ b 的结果再 ^ a ，存入 a 中，此时 b = a, a = b 完成交换
